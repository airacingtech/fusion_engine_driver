/**:
  ros__parameters:
    connection_type: "tcp"
    ip: "10.42.7.211"  # IP address to filter in PCAP (set to "" to disable filtering)
    port: 30200
    device: "/dev/ttyUSB0"
    
    frame_id: "cg"
    debug: false
    
    # PCAP playback mode
    enable_pcap: true
    pcap_file: "/path/to/your/capture.pcap"  # Update this to your PCAP file path
